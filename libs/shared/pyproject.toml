[project]
name = "shared-lib"
version = "0.1.0"
description = "Shared utilities for fintech practice stack"
requires-python = ">=3.11"
dependencies = [
    "pydantic",
    "pydantic-settings",
    "fastapi"
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
